import { FilterToGenerateReports } from '@/features/admin/components/filter-to-generate-reports';

export default async function Page() {
  return (
    <div className="p-2">
      <FilterToGenerateReports />
    </div>
  );
}